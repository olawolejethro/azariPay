# Database (use a separate test database!)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=bongopay_test

# JWT
JWT_SECRET=test-jwt-secret-key-12345
JWT_REFRESH_SECRET=test-refresh-secret-key-12345

# ⚠️ CRITICAL: Copy these from your .env file
# Encryption keys MUST be the same for tests to work!
ENCRYPTION_KEY_PII=<copy-from-your-.env>
ENCRYPTION_KEY_FINANCIAL=<copy-from-your-.env>
ENCRYPTION_KEY_AUTH=<copy-from-your-.env>
ENCRYPTION_KEY_SENSITIVE=<copy-from-your-.env>
HASH_SALT=<copy-from-your-.env>

# Other test config
NODE_ENV=test
PORT=3001